import { JudgeRouteShell } from "@/components/judge-route-shell";

export default function JudgeLayout({ children }: { children: React.ReactNode }) {
  return <JudgeRouteShell>{children}</JudgeRouteShell>;
}
